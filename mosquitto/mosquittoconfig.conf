# Listen on all interfaces (so other containers like Node-RED can connect)
listener 1883
allow_anonymous true

# Optional WebSocket listener (useful later for dashboards)
listener 9001
protocol websockets

# Persistence and logging
persistence true
persistence_location /mosquitto/data/
log_dest stdout